{
  "name": "socketio",
  "version": "1.0.0",
  "description": "socket io support reconnection when server is turned on, but not websocket.",
  "main": "chat-server.js",
  "dependencies": {
    "express": "^4.17.3",
    "socket.io": "^4.4.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
